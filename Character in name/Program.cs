using System;
namespace Character_in_name
{
    internal class Program
    {
        static void Main(string[] args)
        {  
            string firstName = Console.ReadLine();
            string lastName = Console.ReadLine();
            int firstName CharacterCount = firstName.Length;
            int lastName CharacterCount = lastName.Length;
            Console.WriteLine($"{firstName} {lastName}, your first name has{ChacterCount} and your last name has {CharacterCount});
        }
    }
}